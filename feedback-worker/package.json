{
  "name": "my-gather-feedback-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that receives feedback payloads from my-gather reports and creates GitHub issues on the user's behalf.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.5.40",
    "@cloudflare/workers-types": "^4.20251030.0",
    "typescript": "^5.7.3",
    "vitest": "^2.1.9",
    "wrangler": "^4.84.1"
  }
}
